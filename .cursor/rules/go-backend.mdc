---
description: Go 后端开发规范
globs: "**/*.go"
alwaysApply: false
---

# Go 后端规则

## 项目布局

- `cmd/` — 入口程序
- `internal/` — 内部逻辑，不可被外部导入
- `pkg/` — 可复用的公共包

## 错误处理

- 始终显式检查并返回 `error`，不使用 `panic` 处理业务错误
- 使用 `fmt.Errorf("context: %w", err)` 包装错误，保留调用链
- 在最外层统一记录日志，中间层只传递错误

```go
// BAD
data, _ := repo.Find(ctx, id)

// GOOD
data, err := repo.Find(ctx, id)
if err != nil {
    return fmt.Errorf("find user %s: %w", id, err)
}
```

## 接口与依赖

- 接口尽量小，1-3 个方法
- 接口由使用方定义，不由实现方定义
- 通过构造函数注入依赖，便于测试

## 并发

- goroutine 必须有明确的退出机制（context / done channel）
- 共享状态优先用 channel 通信，其次 sync.Mutex
- 使用 `errgroup` 管理并发任务

## API 设计

- RESTful 风格，统一响应结构 `{"code":0,"message":"ok","data":{}}`
- 所有 handler 接收 `context.Context`，用于超时和取消控制
- 中间件处理认证、日志、panic recovery

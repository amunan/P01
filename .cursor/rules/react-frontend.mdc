---
description: React 前端开发规范
globs: "**/*.tsx,**/*.ts,**/*.jsx"
alwaysApply: false
---

# React 前端规则

## 组件规范

- 使用函数式组件 + Hooks，不使用 class 组件
- 组件文件名 PascalCase，如 `UserProfile.tsx`
- 每个文件只导出一个组件，样式就近放置

## TypeScript

- 开启 strict 模式，禁止使用 `any`
- Props 使用 `interface` 定义，导出供外部使用
- 优先用 `unknown` 替代 `any`，配合类型守卫收窄

```typescript
// BAD
const data: any = fetchData();

// GOOD
interface UserData { id: string; name: string; }
const data: UserData = await fetchData();
```

## 状态管理

- 局部状态用 `useState` / `useReducer`
- 跨组件共享用 Context 或 Zustand
- 避免 prop drilling 超过 3 层

## 性能

- 列表渲染必须提供稳定的 `key`
- `useMemo` / `useCallback` 只在有明确性能问题时使用，避免过早优化
- 大列表使用虚拟滚动

## 错误处理

- 页面级组件包裹 ErrorBoundary
- 异步操作统一处理 loading / error / empty 三态

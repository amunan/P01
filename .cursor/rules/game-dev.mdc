---
description: 游戏开发通用规范（引擎无关）
alwaysApply: false
---

# 游戏开发通用规则

引擎尚未确定，以下为引擎无关的通用实践。确定引擎后需补充引擎特定规则。

## 架构

- 优先采用组件化 / ECS 架构，避免深继承层次
- 游戏逻辑与渲染逻辑分离，便于测试和移植
- 模块化设计：输入、物理、音频、UI 各自独立

## 游戏循环

- 更新逻辑使用固定时间步长（fixed timestep）
- 渲染与逻辑帧率解耦
- 避免在更新循环中做 I/O 或重分配操作

## 资源管理

- 资源异步加载，不阻塞主线程
- 使用引用计数或统一的资源管理器控制生命周期
- 场景切换时清理不再使用的资源

## 性能

- 建立帧率预算意识（如 16.6ms/帧 @ 60fps）
- 频繁创建/销毁的对象使用对象池
- 减少每帧的内存分配（GC 压力）

## 状态管理

- 使用有限状态机（FSM）管理游戏状态（菜单 / 游戏中 / 暂停 / 结算）
- 状态转换逻辑集中管理，避免散落在各处

## 输入处理

- 抽象输入层，屏蔽平台差异（键盘 / 手柄 / 触屏）
- 输入映射可配置，支持自定义键位

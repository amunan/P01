---
description: 前后端 API 接口约定
alwaysApply: false
---

# API 约定规则

## URL 设计

- RESTful 风格：`GET /api/users`、`POST /api/users`、`PUT /api/users/:id`
- 资源名使用复数名词，URL 全小写，单词用 `-` 连接
- 版本号放在路径中：`/api/v1/...`

## 统一响应格式

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

- `code: 0` 表示成功，非零为业务错误码
- `message` 为人类可读的提示信息
- `data` 承载实际数据，列表接口包含分页信息

## 错误码规范

- 400 — 参数校验失败
- 401 — 未认证
- 403 — 无权限
- 404 — 资源不存在
- 500 — 服务端内部错误

## 前后端对接

- 后端定义 OpenAPI/Swagger 文档，前端据此生成类型
- 请求/响应字段使用 camelCase
- 时间字段统一 ISO 8601 格式（UTC）
- 分页参数统一：`page`（从 1 开始）、`pageSize`

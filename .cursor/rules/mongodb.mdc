---
description: MongoDB 数据库设计与使用规范
alwaysApply: false
---

# MongoDB 规则

## Schema 设计

- 读多写少 → 优先嵌套（denormalize），减少查询次数
- 写多或数据独立变化 → 使用引用（reference），避免大文档频繁更新
- 单文档上限 16MB，嵌套数组不应无限增长

## 索引策略

- 查询字段必须有索引，避免全集合扫描
- 复合索引遵循 ESR 原则：Equality → Sort → Range
- 定期用 `explain()` 检查查询计划

## Go Driver 约定

- struct 字段使用 `bson` tag 明确映射
- 所有数据库操作传递 `context.Context`，设置合理超时
- 使用 `mongo.IndexModel` 在启动时确保索引存在

```go
type User struct {
    ID    primitive.ObjectID `bson:"_id,omitempty"`
    Name  string             `bson:"name"`
    Email string             `bson:"email"`
}
```

## 查询优化

- 只投影需要的字段，避免返回整个文档
- 聚合管道尽早 `$match` 减少数据量
- 批量操作使用 `BulkWrite`，减少网络往返
